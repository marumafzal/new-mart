You are an expert full-stack engineer. Your task is to fix, refactor, and complete the existing /admin/users module and its connected backend/database logic.

⚠️ RULES (STRICT)
NO NEW FEATURES – Only fix existing broken functionality.

NO DUPLICATE CODE – If a solution requires replacing a broken function, remove the old one completely.

NO TYPESCRIPT ERRORS – All any types must be replaced with proper interfaces.

PRESERVE ALL ROUTES – Do not change API endpoint paths unless they are wrong. If wrong, fix paths but keep names same.

REAL-TIME TRACKING – Maintain PROGRESS.md file. Update it after EVERY small task completion.

MULTI-SESSION READY – Each task must be completable in 5–15 minutes. Agent can stop and resume.

🐞 LIST OF CONFIRMED BUGS (MUST FIX)
#	Bug Description	Expected Behavior
1	User profile picture from Rider/Customer app registration → not showing in admin panel (empty field).	Image URL should display correctly (full path from storage/CDN).
2	Deleting a user from admin → user disappears from UI but not from DB; sometimes shows "band" (banned) state incorrectly.	Soft delete works. After delete, user should not appear in active users list.
3	Ban action on user → does nothing.	Toggle is_banned flag. Banned users cannot log into any app (vendor/rider/customer).
4	Restore action missing for multi-select.	Admin can restore multiple deleted users in one action.
5	Bulk actions (delete, ban, restore) for multiple users → not working.	Select multiple → one API call → all affected.
6	Inconsistent user state between 3 apps (vendor, rider, customer). Admin changes not reflecting across apps.	Centralized users table with role enum. All apps read same is_banned, is_deleted fields.
🧱 TASKS (BREAKDOWN – DO IN ORDER)
✅ TASK 1 – Database Schema Audit & Fix
Files involved: prisma/schema.prisma or models/User.js or migrations/

Ensure users table has:

profile_picture_url (string, nullable)

is_banned (boolean, default false)

is_deleted (boolean, default false)

deleted_at (datetime, nullable)

role (enum: vendor, rider, customer)

If any field missing → create migration.

Update PROGRESS.md: Task 1 done + any changes made.

✅ TASK 2 – Backend API Fixes (User Management)
Files involved: routes/admin/users.js, controllers/admin/userController.js

Fix endpoints:

2.1 GET /admin/users
Return only is_deleted = false users (active).

Include profile_picture_url from DB – do NOT hardcode empty.

2.2 DELETE /admin/users/:id
Soft delete: set is_deleted = true, deleted_at = now().

Do NOT remove from DB.

Return updated user object.

2.3 PUT /admin/users/ban/:id
Toggle is_banned = !is_banned.

If banned → add banned_at timestamp.

2.4 POST /admin/users/bulk-delete
Accept array of user IDs.

Soft delete all in one transaction.

2.5 POST /admin/users/bulk-restore
Set is_deleted = false, deleted_at = null for selected users.

2.6 POST /admin/users/bulk-ban
Set is_banned = true for selected users.

Update PROGRESS.md after each endpoint.

✅ TASK 3 – Fix Profile Picture Display
Files involved: Backend + Frontend admin panel.

On user registration (rider/customer app) – ensure image upload saves full public URL.

In admin panel – render image using <img src="{{user.profile_picture_url}}" />.

If URL is relative, prepend base URL from env STORAGE_URL.

If missing URL → show placeholder (/default-avatar.png) but DO NOT break UI.

Update PROGRESS.md with fix description.

✅ TASK 4 – Admin Frontend (UI/UX Fixes)
Files involved: admin/users/index.vue or .jsx or .tsx (depending on stack)

Fix:

User list table – show profile picture thumbnail (40x40).

Delete button → soft delete + remove row from UI.

Ban button → toggle ban + show badge ("Banned" / "Active").

Bulk actions:

Checkbox column.

Action dropdown (Delete Selected, Restore Selected, Ban Selected).

Call respective bulk API.

Restore button for deleted users (separate tab or filter for deleted users).

Update PROGRESS.md after UI fix.

✅ TASK 5 – Cross-App Consistency (Vendor/Rider/Customer)
Files involved: Auth middleware in each app.

In each app’s login/auth guard – check is_banned and is_deleted from central users table.

If banned → show "Account banned" message, logout, prevent login.

If deleted → show "Account not found".

Do NOT duplicate user logic – use shared service or API call to admin DB.

Update PROGRESS.md after each app fix.

✅ TASK 6 – Real-Time Sync (Optional but Recommended)
If using websockets or polling:

After bulk action → emit event to all apps to refresh user state.

At minimum: invalidate user cache in Redis or local storage.

Skip if no real-time infrastructure exists – but document limitation in PROGRESS.md.

✅ TASK 7 – Testing & Validation (Manual)
Run these checks AFTER all tasks:

Register new rider with profile picture → check admin panel → image shows.

Delete that user → disappears from active list.

Restore from deleted tab → reappears.

Ban user → log in from vendor app → rejected with "banned" message.

Select 3 users → bulk delete → none appear.

Bulk restore → all back.

Bulk ban → all banned.

Write results in PROGRESS.md (Pass/Fail for each).

📁 PROGRESS.md FORMAT (Agent must update this file)
markdown
# Admin Users Module Fix Progress

## Started: [auto timestamp]
## Last Updated: [auto timestamp]

### Task 1: Database Audit
- Status: ✅ Complete / 🔄 In Progress / ❌ Failed
- Changes: [list]
- Errors encountered: [none / details]

### Task 2: Backend API
- 2.1 GET /admin/users ✅
- 2.2 DELETE /admin/users/:id ✅
- 2.3 PUT /admin/users/ban/:id ✅
- 2.4 Bulk delete ✅
- 2.5 Bulk restore ✅
- 2.6 Bulk ban ✅

### Task 3: Profile Picture
- Status: ✅
- Fix: [description]

### Task 4: Admin UI
- Status: 🔄
- Pending: bulk action dropdown

### Task 5: Cross-App
- Vendor app: ✅
- Rider app: ✅
- Customer app: ✅

### Task 6: Real-Time
- Status: ⏸️ Skipped (no WS)

### Task 7: Validation
| Test | Result |
|------|--------|
| Image display | Pass |
| Delete single | Pass |
| Restore | Pass |
| Ban | Pass |
| Bulk delete | Pass |
| Bulk restore | Pass |
| Bulk ban | Pass |

### Agent Log
- Session 1: fixed DB schema and GET users.
- Session 2: fixed soft delete and restore.
...
🚫 WHAT AGENT MUST NOT DO
❌ Create new tables unless required for migration.

❌ Change API response structure that existing apps depend on.

❌ Introduce new npm packages unless absolutely needed (and then list reason in PROGRESS.md).

❌ Leave any // TODO comments without fixing them.

❌ Remove any existing route path – only modify internal logic.

✅ FINAL ACCEPTANCE CRITERIA
Admin can see profile pictures of users from all 3 apps.

Delete, ban, restore work for single AND multiple users.

Banned users cannot log into any app.

Deleted users do not appear in admin active list.

No TypeScript errors.

PROGRESS.md is complete with all steps documented.

